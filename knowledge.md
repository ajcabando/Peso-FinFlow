# Project knowledge

FinFlow — local-first, offline-first personal finance app (Android/iOS/iPadOS/Web). Core idea: a **double-entry accounting engine** — every transaction writes balanced debits/credits to a `ledger_entries` table, and account balances are *derived* aggregates, never stored.

## Quickstart
- Setup: Flutter 3.44+ is at `~/flutter` — `export PATH="$HOME/flutter/bin:$PATH"`, then `flutter pub get`
- Codegen (required after touching Drift tables/DAOs): `dart run build_runner build`
- Dev: `flutter run` (desktop/mobile) or `flutter run -d chrome` (web)
- Test: `flutter test` (currently **229 tests**: unit + repository + widget + stress)
- Lint: `flutter analyze` (must stay clean)
- Build: `flutter build web` → serve `build/web` (DB runs in SQLite WASM via `web/sqlite3.wasm` + `web/drift_worker.js`)

## Architecture
- **Feature-first clean architecture**: `lib/features/<feature>/{domain, data, presentation}` plus `core/` (theme, utils, errors), `database/` (Drift tables/DAOs/seeder), `shared/widgets/` (design system), `app/` (composition root, router, Riverpod providers).
- Dependency flow: **presentation → domain ← data → database → core**. Domain is pure Dart — the double-entry engine has zero Flutter/DB imports and is fully unit-tested.
- **Riverpod** is the DI/composition root (`lib/app/providers/app_providers.dart`). UI reads reactive `StreamProvider`s; repositories/DAOs are provided once. Tests override `databaseProvider` with an in-memory DB (`test/helpers/test_database.dart`).
- **go_router** with `StatefulShellRoute.indexedStack` (Dashboard / Accounts / Settings tabs).
- Key docs: `docs/architecture.md`, `docs/database.md`. Master roadmap is in `README.md`.
- Feature modules added since the original scaffold: `features/bills/` (recurring bills + mark-paid), `features/reports/` (month report, CSV/PDF export), `features/backup/` (portable JSON snapshot), `features/security/` (PIN lock + biometrics). Dashboard watches `billsProvider` — **widget tests must override it** (see `test/helpers/widget_harness.dart`).
- **Profile (2026-08-10)**: `features/profile/` — display name + avatar photo stored in `app_settings` keys `profile.name` / `profile.picture` (NOT under the `security.` local-only prefix → syncs across devices like any setting, and travels with JSON backups). Photos are picked via `file_selector`'s `openFile`, decoded with `dart:ui` `instantiateImageCodec` (handles HEIC), resized to ≤ 384px and re-encoded as JPEG via `package:image` (quality 85) so the stored data URI stays well under the 100 KB per-op sync limit. **Clearing the photo stores `''`** (never deletes the row) so the removal propagates through op-log sync like any value change. Dashboard `_GreetingHeader` greets by first name (`Good morning, Alain`); shared `ProfileAvatar` (`shared/widgets/`) renders the photo or a gradient person fallback; Settings' profile hero card shows name/email + edit. Covered by `test/widget/profile_test.dart` (6 tests).

## Cloud sync (Supabase) — schema v4, added 2026-08-07
> **Deprecated path**: the self-hosted migration plan (`docs/SELF_HOSTED.md` + `docs/BACKEND_API.md`) replaces this Supabase implementation; it stays live until Phase 6 (op-log client lands).
- **Optional, local-first sync**: sign-in lives in Settings → Account & sync. Without `--dart-define=SUPABASE_URL/SUPABASE_ANON_KEY` the feature is disabled and the app is 100% local. `supabase_flutter ^2.17.1` — note the **new API**: `Session` (not `AuthSession`), `Supabase.initialize(publishableKey:)` (not `anonKey:`), gotrue types re-exported. The dashboard's "anon" key IS the publishable key.
- **Architecture** (`docs/SYNC.md`): schema v4 adds nullable `user_id` on accounts/transactions/bills/budgets/tags/app_settings, a `sync_meta` watermark table, and `sync_tombstones` written by SQLite `AFTER DELETE` triggers (in `beforeOpen`). `SyncEngine` (in `lib/features/sync/data/`) does push→pull→converge with **last-write-wins** (`updated_at`), CAS before push (never clobber newer cloud rows), and transaction children (ledger_entries + transaction_tags) replaced as a consistent set. Triggered by sign-in, app resume, 2s-debounced `db.tableUpdates`, a 60s heartbeat, and the manual button.
- **Deterministic seeds**: `DatabaseSeeder` now uses canonical ids from `SeedIds` (`lib/database/seed/seed_ids.dart`) for the system account + 32 categories so all devices merge cleanly. `SeedReconciler` re-points legacy random-id rows during v3→v4 (`insert canonical → re-point ledger_entries.account_id/budgets.category_id → delete legacy` — FK-safe ordering).
- **Security**: `security.*` app-settings keys (PIN hash) are never adopted/pushed. Attachments are not synced (device-local paths). `SyncSession.instance.userId` stamps rows at write time (repos + `SettingsDao.set`).
- **Tests**: `test/sync_engine_test.dart` (FakeSyncRemote, no network) + `test/sync_migration_test.dart`; 193 total. Gotchas hit: drift `notLike` doesn't exist → use `like(...).not()`; drift exports `isNull/isNotNull` that clash with matcher's (hide them in tests); `tableName` column collides with drift's base `Table.tableName` (use `sourceTable`); internal row maps must keep `DateTime` values and only serialize at the wire (`_toRemoteRows`) or LWW casts fail; watermark filters use `!isBefore` (≥) so same-timestamp writes/tombstones aren't skipped.
- **Server**: `supabase/migrations/0001_init.sql` — 8 tables + RLS `for all using (user_id = auth.uid())` + `(user_id, updated_at)` indexes. Phone OTP needs an SMS provider configured in Supabase; email works out of the box.

## Self-hosted backend plan (2026-08-07) — replaces Supabase
- **Master plan**: `docs/SELF_HOSTED.md`; **API contract**: `docs/BACKEND_API.md`. Decision: move off Supabase to a self-hosted **NestJS + Drizzle (Postgres) + MinIO (+ optional Redis)** Docker stack behind the user's nginx. No vendor lock-in; local DB stays authoritative. Phased rollout — Supabase stays the shipped default until the op-log client lands green.
- **Locked choices**: operation-log sync (`POST /sync/push` + `GET /sync/pull`; ops carry `deviceId` verified against the JWT claim; CAS on `base_version` with `SELECT … FOR UPDATE` serialisation + LWW: highest `version` → newest `updated_at` → smallest `op_id`; soft deletes; server-assigned `seq` cursors), Argon2id passwords, JWT access (15m) + opaque rotating refresh tokens bound to devices, email/password only (SMTP via Nodemailer env config; phone OTP dropped), client-encrypted cloud backups (AES-256-GCM + PBKDF2 passphrase key — server is zero-knowledge), MinIO attachments via presigned URLs.
- **Client (schema v5)**: add `sync_outbox` + `version` columns; tombstone triggers feed the outbox; `sync_meta` becomes push/pull `seq` cursors; `FINFLOW_API_URL` define replaces `SUPABASE_*`; new deps `http`/`flutter_secure_storage`/`device_info_plus`/`package_info_plus`/`cryptography`; drop `supabase_flutter` at Phase 6. Sync UI gains device management + backup schedule.
- **Client backup schedule (2026-08-09, Phase 9)**: `backup_passphrase_store.dart` persists the passphrase for unattended runs (secure storage native / localStorage web — same tradeoff as auth tokens); `SyncController` gained `saveBackupPassphrase`/`backupNow` (also records `backup.lastRunAt`)/`setBackupSchedule` (non-manual throws `ValidationException` until a passphrase is stored)/`maybeRunScheduledBackup` (due = signed in + non-manual + passphrase + interval elapsed; failures swallowed, retried next heartbeat); the 60 s `SyncBootstrap` heartbeat fires it alongside the sync tick. Keys `backup.schedule`/`backup.lastRunAt` live in `app_settings` (synced, non-secret). Card: Save-passphrase button, controlled schedule dropdown (guard errors snackbar + value reverts), last-backup display. Covered by `test/backup_schedule_test.dart` (8 tests; fakes: in-memory passphrase store + `implements CloudBackupService` recording fake). Full Flutter suite now **223**; server 40 unit + **88 e2e**.
- **Backend Phase 1 shipped 2026-08-07** (`server/`): NestJS 11 + Drizzle + node-postgres, full 15-table schema in `server/src/drizzle/schema.ts` (migrations `0000`/`0001` generated via `drizzle-kit generate`, applied by the `migrate` compose service). Verified: `flutter`-side untouched, `npm run build` clean, 9 unit + 5 e2e tests green, `docker compose up -d --build` → `/health` + `/health/ready` 200, postgres/minio/redis on an internal network (only `127.0.0.1:8080` published). Stack: `docker-compose.yml` (root) + `server/.env` (app env, `DATABASE_URL=postgres://…@postgres:5432/…`) + root `.env` (compose secrets). Gotchas: drizzle journal lives in the `drizzle` schema (not `public`); npm 11 blocks esbuild postinstall (approve scripts or build works anyway — `nest build` is pure tsc); `devices.id` is the **client-supplied deviceId** (no `defaultRandom`); `refresh_tokens.device_id` indexed; 5xx HttpExceptions are masked + logged by `AllExceptionsFilter`; `ledger_entries/budgets/bills` RESTRICT-FK into `accounts` → sync materializer must soft-delete. nginx block: `deploy/nginx-finflow-api.conf.example`.
- **Backend Phase 2 shipped 2026-08-07** (auth + devices, `server/src/{auth,users,mail,devices}`): Argon2id (OWASP m=19456/t=2/p=1, env-tunable), JWT access (15 min HS256, issuer `finflow`/audience `finflow-api` enforced on sign AND verify; jti = refresh-token row id), opaque refresh tokens (SHA-256 at rest, device-bound, rotated on use via guarded `UPDATE … WHERE revoked_at IS NULL` — no double-successor race; reuse → whole-device chain revocation; `REFRESH_TTL_DAYS=30`), email verification (optional; `EMAIL_VERIFICATION_REQUIRED=true` fails boot without SMTP; enforced 60-min token TTL) + SMTP password reset (Nodemailer; unconfigured SMTP = `sent:false` no-op), device registry (`devices.id` = client-supplied `deviceId`, re-activation on re-login, owner-scoped revoke → 404 for other users), per-IP (5/min) + per-email (10/h) in-memory brute-force limiter (Redis in Phase 8). `JWT_ACCESS_SECRET` (≥32 chars) now required — generated into `server/.env` (gitignored). Verified: build clean, 24 unit + 27 e2e tests green (real Postgres via `docker-compose.test.yml` which publishes 127.0.0.1:54321), live curl flows OK, **stack left running** on 127.0.0.1:8080. Gotchas: drizzle wraps pg errors (`error.cause.code` — unique-violation detection must walk the cause chain); Nest has no `TooManyRequestsException` (use `HttpException(…, 429)`); **Docker Desktop cannot publish ports for containers attached only to an `internal:true` network** (test override joins the proxy network to fix); login equalises timing with a dummy Argon2 verify (no user enumeration); reset response `{sent}` reflects only `mail.enabled` (never account existence).
- **Backend Phase 3 shipped 2026-08-07** (op-log sync engine, `server/src/sync/`): `POST /sync/push` (batch ≤ 500, ≤ 100 KB/op payload) + `GET /sync/pull?cursor&limit` (repeatable, immutable, parent-first order). Push = one tx: per-(user,entity,entityId) `pg_advisory_xact_lock` serialisation (NOT `SELECT … FOR UPDATE` on the current-state row — can't lock fresh rows; locks acquired in deterministic `(priority, entityId)` order to avoid AB-BA deadlocks), idempotent by `op_id`, CAS on `base_version`, LWW (version → updated_at → op_id); losers are NEVER inserted and reported as **conflicts** with `current` (NOT "applied no-ops" — acked-winner-seq would jump the client cursor past the winner); **conflicts never advance `serverCursor`** (0 if nothing applied). Atomicity: in-body conflicts = partial apply; thrown errors (LEDGER_IMBALANCE 409, malformed 400, FK `23503` → 409) roll back the whole batch. Device row re-checked `FOR UPDATE` in-tx (revoke TOCTOU). `SyncMaterializer` upserts/soft-deletes the current-state mirror + atomically replaces transaction children (soft-delete of a txn hard-deletes its ledger entries so balances stay clean; accounts only ever soft-deleted). `assertBalancedLedger` mirrors the Dart engine (≥2 entries, positive safe-int amounts, debits==credits). **Composite PKs `(user_id, id)`** on accounts/transactions/bills/budgets/tags/ledger_entries (+`(user_id,transaction_id,tag_id)` transaction_tags; `(user_id,key)` app_settings) with composite FKs via the `foreignKey()` helper — CRITICAL: the app seeds deterministic ids (SeedIds) on every install, so global-id PKs made any two users collide (23505) and let unscoped `eq(id)` reads mutate other users' rows; **every mirror query must be `WHERE user_id = ? AND id = ?`**. Migrations `0002` (drop version-unique index → latest-seq-is-current + `(user_id,entity,entity_id,seq)` index; the unique index 23505'd on legit LWW-winner inserts), `0003` (composite PKs — drizzle-kit can't auto-name PK drops, hand-filled `<table>_pkey`; `transaction_tags` had no old PK), `0004` (`attachments` FK CASCADE — was SET NULL, which would null NOT NULL `user_id` and break the users→transactions cascade). Shared bootstrap `src/app.setup.ts` (`configureApp`: global prefix + 2 MB JSON body limit — 500 full payloads exceed the 100 KB body-parser default; used by main.ts AND all e2e harnesses). E2e now runs `maxWorkers: 1` (4 suites share one test Postgres; parallel workers raced on TRUNCATE). Verified: build clean, 36 unit + **49 e2e** (real Postgres; new cross-user same-entity-id regression, mixed good/bad batch fail-fast, orphan-account 409), live curl smoke + two-user same-category smoke (2 rows, 0 conflicts), migration upgrade 0000→0004 on a persisted volume, user-deletion cascade test. **Stack left running** on 127.0.0.1:8080.
- **All-phase review (2026-08-08)**: deep review of Phases 1–3 fixed 6 findings — (1) **strict money coercion** in the materializer (`int`/`optInt`/`bool` now throw `400` on present-but-invalid values instead of silently defaulting — a web client serialising a big Dart int as a JSON string would otherwise write `opening_balance_minor: 0`); (2) **duplicate `transactionTags` deduped** (was 23505 → 500); (3) **generated ledger-entry ids** loop past explicit-id collisions (`while (seenIds.has(id))` — a client choosing `<txnId>-1` for entry 0 used to collide with the generator); (4) **opId reuse across different entities → `409 CONFLICT`** (the idempotency short-circuit silently acked a replayed opId, leaving the second entity permanently unmaterialised); (5) **`serverCursor` is `null` when nothing applied** (never `0` — `0` is the pull "fresh" sentinel; docs §4 updated); (6) **new password-reset request invalidates all prior tokens** (usedAt-stamp inside a tx). Plus ops: `23505` duplicate-key → `409` (was 500); compose gains `restart: unless-stopped` on postgres/minio/redis + MinIO pinned by digest. Test count: 36 unit + **56 e2e** (7 new regressions). Live smoke: string-money 400, dup-tag dedupe, opId-reuse 409, all-conflict `serverCursor: null` — all PASS.
- **Backend Phase 4 + hardening shipped 2026-08-08** (`server/src/{storage,attachments,backups,resources,ledger,redis,health}`): MinIO module (private bucket bootstrapped on boot, presigned PUT/GET, `stat`, digest-pinned image) + **two-step upload confirm** for attachments (`POST /attachments` → presigned PUT → `PATCH {uploaded:true}`) and backups (`POST /backups` → PUT → PATCH → prune past `BACKUP_RETENTION`); **confirm is server-verified via `statObject`** — 404 if never uploaded, 400 if the actual blob exceeds the declared `sizeBytes` OR `MAX_ATTACHMENT_BYTES`/`MAX_BACKUP_BYTES` (a presigned PUT can't bound the upload; over-limit blobs are deleted). Resources module (`GET/POST/PATCH/DELETE /accounts /transactions /bills /budgets /tags /settings` + `PUT /settings` batch) — writes **generate ops through the sync engine** (single write path), reads scoped `(user_id, id)` + `deleted_at IS NULL`, `security.*`/`pin.*` settings rejected server-side. `/ledger` derives balances from `ledger_entries` (never cached), skips soft-deleted transactions' entries. **Redis-backed brute-force limiter** (`ioredis`, `lazyConnect` + explicit background `connect()`, fail-open, `get` NaN-guarded, unconditional EXPIRE in the incr pipeline) with identical in-memory fallback; `/health/ready` now checks minio+redis (conditional on config; probes race a 2s timeout so a black-holed network degrades instead of hanging); pino `redact` + custom `req` serializer strips query strings (reset tokens in URLs) + auth headers. Gotchas: minio-js v8 throws `S3Error` with **`code: 'NotFound'`** (not `NoSuchKey`) for a missing stat — `isMissingKeyError` accepts both (bit the health probe AND confirm mapping); ioredis `lazyConnect:true` never connects without an explicit `connect()` (commands fail "Stream isn't writeable" → health redis down); Docker-internal `MINIO_ENDPOINT=minio` bakes an unreachable host into presigned URLs for host-side curl — smoke tests upload via `docker compose exec minio curl`; e2e suites pin `REDIS_URL=''`/`MINIO_ENDPOINT=''` (non-storage suites) and the storage suite loads MinIO creds from the root `.env` (test override publishes `127.0.0.1:19000`); compose Redis `requirepass` is conditional on `REDIS_PASSWORD` (set it → `REDIS_URL` must carry the password). Verified: build clean, **40 unit + 85 e2e** (real Postgres + real MinIO; new: confirm-404/400s, declared-size 400), **31 live smoke checks PASS**, compose valid, stack healthy (postgres/minio/redis up). **Stack left running.**
- **App Store note**: shipping sync invalidates the "No Data Collected" ASC privacy answer — update before going live (tracked in `dist/ASC_SUBMISSION_CHECKLIST.md`).

## Performance & pagination
- The transactions list is **windowed** (30 rows per page, infinite scroll): `transactionsPage({from, to, search, type})` in `TransactionDao` + `TransactionListController`. DB-level search uses case-insensitive `LIKE` on merchant/note; `existsQuery` flags whether more rows remain.
- The `test/performance_stress_test.dart` harness seeds 50k+ transactions and asserts pagination + aggregate latency stays reasonable.

## Database (Drift/SQLite, schema v3)
- All monetary values are **integers in minor units** (12345 = ₱123.45). Decimal digits come from `CurrencyFormatter` (JPY=0, BHD=3, default 2). Never use `double` for money.
- Income/expense **categories live in the `accounts` table** (`kind = category`) — category spend is just a ledger aggregate, same as account balances.
- `accounts.opening_balance_minor` is informational only; the authoritative opening balance is a real "Opening Balance" transaction paired with the system account.
- Every write is validated by `DoubleEntryEngine` (debits == credits, ≥2 entries, positive amounts) and wrapped in `db.transaction(...)` for atomicity.
- `DatabaseSeeder` (idempotent, `onCreate`) seeds the Opening Balances system account + 32 default categories.
- **Gotcha**: editing tables/DAOs requires `dart run build_runner build` to regenerate the committed `.g.dart` files; schema changes need a version bump + migration step in `AppDatabase`.
- **Backup**: `BackupService` exports a portable JSON snapshot via a custom enum-aware `ValueSerializer` (drift 2.34 dropped its bundled backup API — do not rely on `ExportDatabase`/`ImportDatabase`). Round-trip is covered by `test/backup_service_test.dart`.
- **Security**: PIN is salted SHA-256 via `PinService` (pepper in `AppConstants`); `SecurityGate` wraps the app root. `local_auth` is v3 (flat params, no `AndroidOptions`). Native platform config lives in `ios/Runner/Info.plist`, `macos/Runner/*.entitlements`, `android/.../AndroidManifest.xml`.
- **UI gotcha (2026-08-09)**: `AppButton` defaults to `expand: true` (wraps in `SizedBox(width: double.infinity)`) — placing one directly inside a `Row` (a non-flex child gets unbounded max width) crashes with `BoxConstraints forces an infinite width` during layout. Use `expand: false` for inline buttons or wrap in `Expanded`. This bit the Settings → Account & sync card's inline "Sign in" button (crashed the whole Settings page whenever sync was enabled and signed out); covered by `test/widget/settings_sync_section_test.dart`.
- **App name (v0.1.0+6)**: Store/brand name is **Peso-FinFlow**. iOS display name = `ios/Runner/Info.plist` (`CFBundleDisplayName`); Android label = `AndroidManifest.xml`; macOS product name = `macos/Runner/Configs/AppInfo.xcconfig` (`PRODUCT_NAME`). In-app branding (dashboard header "FinFlow", CSV header) is separate and test-asserted. Note: renaming macOS `PRODUCT_NAME` renames the built artifact (`.app`/`.ipa`) — `dist` copies must follow the new name.

## Machine gotchas (this Mac)
- **Never symlink `~/Library/Developer/CoreSimulator` (or `~/Library/Developer/Xcode`) to another volume** — the sandboxed `CoreSimulatorService` cannot write through the redirect (EPERM), so simulator device creation fails and *every* iOS build (ibtool/actool) dies with `Failed to find or create execution context` / `Failed to find a suitable device`. This bit us on 2026-08-06 (data was redirected to `/Volumes/DATA/Dev/`); the fix was copying the data back to the real system-volume paths and removing the symlinks. The DATA copies still exist at `/Volumes/DATA/Dev/` if you need to restore anything. Xcode itself living on the DATA volume is fine — only the `~/Library/Developer/*` redirects break.
- iOS project is now **storyboard-free** (modern Flutter template): `UILaunchScreen` in `Info.plist`, no `Main.storyboard`/`LaunchScreen.storyboard`. This was required because this machine's CoreSimulator can't run ibtool.
- App Store upload: `dist/FinFlow-v0.1.0-appstore.ipa` (24 MB, App Store distribution signing, team 25FHU3ZF38). Upload via `xcrun altool --upload-app --type ios -f dist/FinFlow-v0.1.0-appstore.ipa -u <appleid> -p <app-specific-password>` or the Transporter app.

## iOS build & App Store (lessons learned)
- **Never remove `Main.storyboard`**: `FlutterSceneDelegate` needs `UISceneStoryboardFile: Main` to instantiate the `FlutterViewController`. A storyboard-free setup (dict-only) compiles, validates and uploads fine but renders a **blank screen and Dart never starts** on device/simulator. If it ever regresses, restore with `python3 tool/fix_ios_storyboard.py`. Symptom: app process alive, no crash, no DB created, black screen.
- **Screenshot workflow** (native, no web hacks): `flutter build ios --simulator --debug --dart-define=FINFLOW_DEMO_DATA=true --dart-define=FINFLOW_SCREEN=<analytics|transactions|accounts>` (the `FINFLOW_SCREEN` define in `main.dart` auto-navigates after launch; omit it for the dashboard). Install via `xcrun simctl install`, launch, `simctl io <udid> screenshot /tmp/x.png` (never write to the DATA volume — simctl can't). iPhone 15 Pro Max sim = native 6.7" 1290×2796; iPad 13" M4 sim → downscale 2064×2752 → 2048×2732 (PIL LANCZOS).
- **fl_chart implicit animations (`duration:`) hang the first frame on web** (and software-rendered sims): charts render a perpetual boot splash (`#c4c4c4`). The charts in `cash_flow_chart.dart`, `net_worth_trend_chart.dart`, `category_spend_section.dart` use `duration: Duration.zero` deliberately.
- Deployment target is **iOS 15.0** (Apple requires ≥15 by Spring 2027; also the minimum for `UILaunchScreen` dict + `UIColorName`).
- App Store name is **Peso-FinFlow** (bundle `com.finflow.finflow`). Build 2 was broken (no storyboard); build 3 fixed that; **build 4 is current** (switched the app icon to the user's `dist/Peso-FinFlow.png` design — 1254px white/ink-teal master; regenerate all sizes by downscaling it to `dist/finflow_icon_1024.png`, writing the 15 `Icon-App-*.png` iOS sizes, and running `tool/generate_platform_icons.py` for Android/macOS).

## App Store Connect automation
- Credentials live in **`~/.config/finflow/asc.env`** (gitignored, chmod 600): `ASC_ISSUER`, `ASC_KEY_ID`, `ASC_KEY_PATH` (`.p8` at `~/.config/finflow/AuthKey_*.p8`), `ASC_APP_ID` (Peso-FinFlow = `6798621116`). Created 2026-08-06 with an Admin API key. **Rotated 2026-08-06** — key `H3LV6VJW7W` (exposed in chat) replaced by `S97DXUDGG3`; old key revoked in ASC and local copies deleted.
- Helper: **`tool/asc_api.py`** — lists builds (`builds`), finds apps (`apps`), deep-dive on a build (`detail <build_id>`: processing state, beta review state, beta groups, export compliance), adds a build to the 'Internal Testers' beta group (`betagroup <build_number>`), and polls until ready (`watch [build_number]`). Usage: `python3 tool/asc_api.py builds` / `detail e7d27ac4-…` / `betagroup 7` / `watch 7`. Note: single-resource endpoints return `data` as a dict, list endpoints as an array.
- **GitHub**: repo **renamed `ajcabando/FinFlow` → `ajcabando/Peso-FinFlow`** (2026-08-09; old name auto-redirects for git/API/releases — `git remote` shows the new URL as the canonical one) and is **PUBLIC** with **Pages LIVE** — privacy policy serves at **`https://ajcabando.github.io/Peso-FinFlow/privacy-policy.html`** (HTTP 200, title "Peso-FinFlow — Privacy Policy"). Pages source = branch-based `gh-pages` (`build_type: legacy`; branch holds only `privacy-policy.html` + `.nojekyll`). gh CLI is authenticated as ajcabando (release/Pages/API all work).
  - **Gotcha — rename kills Pages + no redirect**: the rename left the repo **private** (GitHub does NOT carry visibility over), which disabled Pages — old and new `github.io` URLs both 404'd and `POST /pages` failed with 422 "Your current plan does not support GitHub Pages for this repository". Also **GitHub does NOT redirect old Pages URLs after a rename** — `https://ajcabando.github.io/FinFlow/privacy-policy.html` stays 404 forever. Fix used: `gh repo edit ajcabando/Peso-FinFlow --visibility public --accept-visibility-change-consequences` (note the flag is `--accept-visibility-change-consequences`, not `--confirmation`), then `POST /repos/ajcabando/Peso-FinFlow/pages` with `{"build_type":"legacy","source":{"branch":"gh-pages","path":"/"}}` (a fully-removed site needs POST, not PUT); status shows `errored` transiently right after creation but flips to `built` in ~15 s.
  - **Gotcha — Pages hybrid state wedges deployments**: source pointing at `gh-pages` while `build_type=workflow` left the site `errored` and every deploy (workflow `deploy-pages@v4` AND branch builds) hung at `deployment_queued` until the 10-min timeout. Fix: `PUT /repos/{owner}/{repo}/pages` with `{"build_type":"legacy","source":{"branch":"gh-pages","path":"/"}}`, then push to `gh-pages` to trigger the rebuild (changing the source alone does NOT trigger one). `DELETE /pages` is blocked (422 "Deactivating GitHub Pages for this repository is not allowed"). The `pages.yml` workflow is now redundant but harmless.
- **v0.2.0**: pubspec bumped to `0.2.0+1`; next-release plan in `docs/NEXT_RELEASE.md` (P0: multi-currency, undo, scheduled transactions; P1: filters, YTD reports, widgets).
- **Gotcha — low-S signatures**: JWT signing must use ES256 with **low-S normalization**. OpenSSL's random nonce produces high-S signatures ~50% of the time and Apple rejects them with HTTP 401 (`NOT_AUTHORIZED`) — the auth appears intermittently broken. Also strip DER's leading `0x00` padding bytes before padding r/s to 32 bytes. Both are handled inside `asc_api.py` — don't reimplement.
- Build 6 (`0.1.0 (6)`) was uploaded 2026-08-06 and is **VALID** on App Store Connect (min iOS 15.0, `usesNonExemptEncryption=False`, expires 2026-11-04). Uploads via `xcrun altool --upload-app` with the app-specific password (see chat history — do not store in repo).
- ASC metadata copy ready in **`dist/ASC_COPY.md`** (description, keywords, what's new, privacy answers) + **`dist/privacy-policy.html`** (host on GitHub Pages, e.g. `https://ajcabando.github.io/FinFlow/privacy-policy.html`). Screenshots + 1024px icon packaged in `dist/Peso-FinFlow-ASC-assets.zip`.

## Go-live status (2026-08-06)

**Peso-FinFlow v0.1.0 (build 6) is fully staged for App Store submission** — everything settable via the ASC API is DONE:

- iOS app store version **0.1.0** (`3a28a91e-…`) in `PREPARE_FOR_SUBMISSION`; **build 6 attached** (version string renamed from "1.0" to "0.1.0" via `PATCH /v1/appStoreVersions` to match the build).
- en-US localization filled via API: description, keywords (93 chars — the copy-pack's 118-char list exceeds the 100 limit, trimmed), promotionalText, supportUrl. **`whatsNew` is blocked via API** (409 `STATE_ERROR` "cannot be edited at this time") — not needed for a first release, set in the UI if desired.
- App info: name `Peso-FinFlow`, subtitle `Know where every peso goes`, `privacyPolicyUrl` set to the live Pages URL — **updated 2026-08-09** to `https://ajcabando.github.io/Peso-FinFlow/privacy-policy.html` (old `…/FinFlow/…` URL 404s; `privacyPolicyUrl` lives on the en-US `AppInfoLocalization`, patched via `PATCH /v1/appInfoLocalizations/{id}` — the one-off script `asc_api._request`/`_jwt` helpers can do this without extra tooling).
- Age rating 4+: **mixed schema** — 13 string-enum fields (`alcoholTobaccoOrDrugUseOrReferences`, `gamblingSimulated`, `sexualContentGraphicAndNudity`, `violenceRealistic`, `horrorOrFearThemes`, `profanityOrCrudeHumor`, `violenceRealisticProlongedGraphicOrSadistic`, `contests`, `matureOrSuggestiveThemes`, `gunsOrOtherWeapons`, `violenceCartoonOrFantasy`, `medicalOrTreatmentInformation`, `sexualContentOrNudity` → `"NONE"`), 7 booleans (`gambling`, `lootBox`, `messagingAndChat`, `socialMedia`, `unrestrictedWebAccess`, `userGeneratedContent`, `advertising` → `false`) + required `parentalControls`/`healthOrWellnessTopics`/`ageAssurance` → `false`. Apple returns TWO errors per wrong field (type + value) — dedupe per field when iterating.
- **Screenshots uploaded via API** (chunked asset upload): 4× `APP_IPHONE_67` + 4× `APP_IPAD_PRO_129` sets under the en-US localization. Gotchas: attribute is `screenshotDisplayType` (NOT `displayType`), `appScreenshots` POST requires `fileName` + `fileSize`, upload = PUT each `uploadOperations` chunk (S3 presigned) then `PATCH appScreenshots` with `sourceFileChecksum` (md5 hex) + `uploaded:true`. Set creation is idempotent-ish: duplicate display type 409s.
- Export compliance already `usesNonExemptEncryption=false`. `ageRatingDeclarations` GET_INSTANCE is 403 (UPDATE only).

**Still manual (ASC UI only, no public API):** ① App Privacy → "No Data Collected" questionnaire; ② App Review contact info (needs the account owner's phone/email); ③ the final **Submit for Review** click + content-rights question (Apple removed programmatic submission in 2023); ④ optional: delete stale builds 2–5, add What's New text. First review takes 24–72h. Follow-up: `tool/asc_api.py` has no submit command — the UI click is required.

## Web app + Docker (2026-08-06)

The Flutter web target is the **same UI** — it ships SQLite WASM (`web/sqlite3.wasm` + `web/drift_worker.js` via `WasmDatabase.open`, conditional import in `lib/database/`). The web build is now PWA-installable and Docker-built, **nginx-free** (the user's host nginx serves it):

- **PWA shell**: `web/index.html` + `web/manifest.json` branded (Peso-FinFlow, seed `#6D5DF6`), iOS meta + `apple-touch-icon.png` (180px) for "Add to Home Screen", manifest 192/512 + maskable icons. Icons regenerated by **`tool/generate_web_icons.py`** from `dist/finflow_icon_1024.png`. Build with **`flutter build web --release --pwa-strategy=offline-first`** → generates `flutter_service_worker.js` (Android install prompt + offline).
- **Docker**: `Dockerfile` is build-only — `debian:bookworm-slim` pinned **`--platform=linux/amd64`** (Apple Silicon! Flutter ships only x86_64 Linux SDKs; without the pin the dart binary dies with `rosetta error: failed to open elf`), installs the **pinned Flutter 3.44.8 SDK** from `storage.googleapis.com` (third-party images like `cirruslabs/flutter:stable` are stale — Dart 3.12.0 < the project's `^3.12.2`), runs `flutter build web --release --pwa-strategy=offline-first`, exports to a `scratch` artifact. Requires `git config --global --add safe.directory /opt/flutter` after extraction. Build with `docker build -o build/web .` (or `./tool/build_web_docker.sh [demo]`), then serve `build/web` with the user's nginx per **`deploy/nginx-finflow.conf.example`** (`application/wasm` MIME in a scoped location, immutable caching for `/assets/`+`.wasm`, no-cache for `flutter_service_worker.js` and `/`, SPA `try_files … /index.html`). `.dockerignore` excludes `dist/` (26MB IPAs) etc.
- **Gotcha — localhost IPv6**: `tool/serve_web.py` binds `127.0.0.1` only; Chrome resolves `localhost` to `::1` first → connection-error page. Use `http://127.0.0.1:8080` (or bind `0.0.0.0`) for checks.
- **Verified 2026-08-06**: analyze clean, 180 tests pass, web build compiles, docker export = 48MB site, headless Chrome renders the dark-theme UI. Remaining manual: real-browser persistence check (OPFS), SW registration, mobile install prompts, and the user's nginx config. Full plan/gotchas: `docs/WEB_APP.md`.

## Conventions
- Linting: `flutter_lints` via `analysis_options.yaml`.
- Material 3 design system with design tokens in `lib/core/theme/` (`app_palette.dart`, `app_colors.dart`, `app_typography.dart`, `app_spacing.dart`, `app_radii.dart`, `app_shadows.dart`); Inter font family (bundled in `assets/fonts/`).
- Errors: all failures are `FinFlowException` subclasses (`ValidationException`, `DomainException`, `NotFoundException`, `DatabaseException`) — UI surfaces `message` in a snackbar, never leaks internals.
- Money parsing/formatting goes through `MoneyInputParser` and `CurrencyFormatter` in `core/utils/` — don't reimplement.
- Tests: widget tests use `test/helpers/widget_harness.dart` (overrides DB + providers); repositories tested against an in-memory DB.
