# npm 11 blocks install scripts by default. argon2 needs its native binary
# (node-pre-gyp download); allow its script so both local installs and the
# Docker `npm ci` runs it. esbuild resolves its binary via optionalDependencies
# and needs no approval.
allow-scripts=argon2
